package com.devproject.collectionexample.list;

//class Calculator<T> {
//    public T add(T a, T b){
//        return a + b;   // ->  Error

//        Errors in Code
//        Operator + Cannot Be Used on Generic Types (T a + T b)
//
//        The + operator only works with primitive types like int, double, etc.
//                Since T is a generic type, the compiler doesn't know if T supports the + operation.
//        Solution: Use Number and Explicit Type Conversion
//
//        Instead of a generic T, restrict T to extend Number (T extends Number).
//        Convert T to a known number type (like double or int) before performing the addition.
//    }
//}


public class GenericExample {
    public static void main(String[] args) {
//        Calculator<Integer> calculator = new Calculator<>();

//        System.out.println(calculator.add(10,20));
    }
}
